# Reference materials

This folder holds the paper-related reference documents:

- `rpm_paper_draft.docx` — the manuscript draft submitted to the original venue (pending upload)
- `reviewer_comments.docx` — the three reviewer reports + metareview from that submission (pending upload)
- `supplementary_materials/` — a snapshot of the backend code as it stood at the time of the original paper submission (kept for provenance/reproducibility of that submission, not the live codebase — see `/backend` for current code)
- `supplementary_materials.zip` — the packaged version of the above as submitted

Once the two `.docx` files are added here, `PROJECT_AUDIT.md`'s Phase 0.5 section should be re-run against the actual reviewer language.
